-- Copyright (c) 2022 Mark Johnston <markj@FreeBSD.org>

Params = {
}

Inputs = {
    checkout = {
        task = "git/checkout",
        params = {
            repo = "https://github.com/freebsd/pkg",
            branch = "master",
        }
    }
}

function Run(ctx, params, inputs, outputs)
    -- XXX-MJ replace hard-coded "checkout" path
    system("checkout/configure")
    system("make -j " .. ctx.maxjobs)
end

-- vi: ft=lua
